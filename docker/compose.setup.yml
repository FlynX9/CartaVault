name: cartavault-setup

services:
  setup:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        CARTAVAULT_VERSION: ${CARTAVAULT_VERSION:-0.9.0-beta.1}
        VITE_API_BASE_URL: /api
    image: cartavault:${CARTAVAULT_VERSION:-0.9.0-beta.1}
    user: "0:0"
    entrypoint: ["python", "-m", "app.setup_cli"]
    environment:
      CARTAVAULT_ENV_FILE: /config/.env
    volumes:
      - ${CARTAVAULT_ENV_FILE:-./.env}:/config/.env
