export interface KmzPreviewImage { internal_id: string; original_name: string; mime_type: string; size: number; source_type: 'embedded' | 'remote_supported' | 'remote_unsupported' | 'unavailable'; host: string | null }
export interface KmzPreviewItem { source_index: number; selected_by_default: boolean; name: string | null; latitude: number | null; longitude: number | null; altitude: number | null; mapped_fields: Record<string, string>; custom_fields: Record<string, string | string[]>; images: KmzPreviewImage[]; warnings: string[]; errors: string[]; importable: boolean; already_imported: boolean }
export interface KmzPreview { import_id: string; file_name: string; placemark_count: number; valid_count: number; warning_count: number; error_count: number; items: KmzPreviewItem[]; global_warnings: string[] }
export interface KmzImportReport { created_count: number; skipped_count: number; error_count: number; images_added: number; embedded_images_added: number; remote_images_added: number; remote_images_unavailable: number; created_place_ids: string[]; failures: { source_index: number; message: string }[]; warnings: string[] }
