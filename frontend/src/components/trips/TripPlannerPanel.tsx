import { useEffect, useState, type DragEvent } from 'react'
import { ArrowDown, ArrowUp, BedDouble, CalendarPlus, Check, Copy, Download, GripVertical, Lock, MapPin, Navigation, Pencil, Plus, Route, Save, Trash2, X } from 'lucide-react'

import { addTripDay, addTripDeparture, addTripNight, addTripStop, calculateTripDayRoute, completeTrip, confirmTripOptimization, createTrip, deleteTrip, deleteTripDay, deleteTripDeparture, deleteTripNight, deleteTripStop, duplicateTrip, duplicateTripDay, exportTripGoogleMaps, exportTripGpx, exportTripKmz, getTrip, listTrips, moveTripStop, optimizeTripDay, reorderTripDays, reorderTripStops, startTrip, tripExportUrl, updateTrip, updateTripDeparture, updateTripNight } from '../../api/trips'
import type { PoiMap } from '../../types/map'
import type { Trip, TripDay, TripOptimization } from '../../types/trip'
import { CreateTripDialog } from './CreateTripDialog'
import { CreateTripNightDialog } from './CreateTripNightDialog'

interface Props { poiMap: PoiMap; trip: Trip | null; activeDayId: string | null; onTripChange: (trip: Trip | null) => void; onActiveDayChange: (id: string | null) => void; onStopFocus?: (latitude: number, longitude: number) => void; onClose: () => void }

export function TripPlannerPanel({ poiMap, trip, activeDayId, onTripChange, onActiveDayChange, onStopFocus, onClose }: Props) {
  const canEdit = poiMap.can_edit === true
  const [trips, setTrips] = useState<Trip[]>([]); const [optimization, setOptimization] = useState<{ dayId: string; value: TripOptimization } | null>(null)
  const [draftName, setDraftName] = useState(''); const [dirty, setDirty] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null)
  const [createOpen, setCreateOpen] = useState(false)
  const [draggedStopId, setDraggedStopId] = useState<string | null>(null)
  const [dropTarget, setDropTarget] = useState<{ dayId: string; index: number } | null>(null)
  const [routeFeedback, setRouteFeedback] = useState<string | null>(null)
  const reload = async (id = trip?.id) => { const items = await listTrips(poiMap.id); setTrips(items); const target = id && items.some((item) => item.id === id) ? id : items[0]?.id; if (!target) { onTripChange(null); onActiveDayChange(null); return }; const loaded = await getTrip(target); onTripChange(loaded); setDraftName(loaded.name); setDirty(false); onActiveDayChange(loaded.days.some((day) => day.id === activeDayId) ? activeDayId : loaded.days[0]?.id ?? null) }
  useEffect(() => {
    let active = true
    void listTrips(poiMap.id).then(async (items) => {
      if (!active) return
      setTrips(items)
      if (!items[0]) { onTripChange(null); onActiveDayChange(null); return }
      const loaded = await getTrip(items[0].id)
      if (!active) return
      onTripChange(loaded); setDraftName(loaded.name); setDirty(false); onActiveDayChange(loaded.days[0]?.id ?? null)
    }).catch((caught: unknown) => { if (active) setError(caught instanceof Error ? caught.message : 'Chargement impossible.') })
    return () => { active = false }
  }, [poiMap.id, onActiveDayChange, onTripChange])
  const run = async (action: () => Promise<void>) => { setBusy(true); setError(null); try { await action() } catch (caught) { setError(caught instanceof Error ? caught.message : 'Opération impossible.') } finally { setBusy(false) } }
  const reorderDays = (index: number, delta: number) => { if (!trip) return; const ids = trip.days.map((day) => day.id); const target = index + delta; if (target < 0 || target >= ids.length) return; [ids[index], ids[target]] = [ids[target], ids[index]]; void run(async () => { await reorderTripDays(trip.id, ids); await reload(trip.id) }) }
  const reorderStops = (day: TripDay, index: number, delta: number) => { const ids = day.stops.map((stop) => stop.id); const target = index + delta; if (target < 0 || target >= ids.length) return; [ids[index], ids[target]] = [ids[target], ids[index]]; void run(async () => { await reorderTripStops(day.id, ids); await reload(trip!.id) }) }
  const drop = (event: DragEvent, day: TripDay) => { event.preventDefault(); const data = event.dataTransfer.getData('text/plain'); if (data.startsWith('place:')) void run(async () => { await addTripStop(day.id, { place_id: data.slice(6), stop_type: 'place', visit_duration_minutes: 30 }); await reload(trip!.id) }); if (data.startsWith('stop:')) void run(async () => { await moveTripStop(data.slice(5), day.id, day.stops.length); await reload(trip!.id) }) }
  const dropStop = (event: DragEvent, day: TripDay, index: number) => { event.preventDefault(); event.stopPropagation(); const stopId = event.dataTransfer.getData('text/plain').replace(/^stop:/, ''); setDraggedStopId(null); setDropTarget(null); if (!stopId) return; void run(async () => { await moveTripStop(stopId, day.id, index); await reload(trip!.id) }) }
  const recalculateRoute = (day: TripDay) => void run(async () => { await calculateTripDayRoute(day.id); await reload(trip!.id); setRouteFeedback(day.id); window.setTimeout(() => setRouteFeedback((current) => current === day.id ? null : current), 1600) })
  const exportFile = async (kind: 'gpx' | 'kmz') => { const item = kind === 'gpx' ? await exportTripGpx(trip!.id) : await exportTripKmz(trip!.id); window.open(tripExportUrl(item.download_url), '_blank', 'noopener,noreferrer') }

  return <aside className="map-sidebar trip-planner-panel" aria-label="Préparation de sortie"><header className="trip-panel-header"><div><p className="cv-workspace-panel__eyebrow">Sortie</p><h2>Préparation</h2></div><button className="panel-icon-button" type="button" aria-label="Fermer le panneau Sortie" onClick={onClose}><X size={17} /></button></header>{error && <p className="trip-panel-error" role="alert">{error === 'Internal Server Error' ? 'Une erreur serveur empêche cette opération.' : error}</p>}<div className="trip-panel-selector"><select aria-label="Voyage actif" value={trip?.id ?? ''} onChange={(event) => void reload(event.target.value)}><option value="">Choisir un voyage</option>{trips.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select>{canEdit && <button className="panel-icon-button primary" type="button" aria-label="Créer une sortie" onClick={() => setCreateOpen(true)}><Plus size={16} /></button>}</div>
    {!trip ? <div className="trip-panel-empty"><Route size={28} /><strong>Aucune sortie préparée</strong><span>Créez un voyage puis ajoutez les POI depuis le panneau Lieux.</span></div> : <><div className="trip-panel-fields"><input aria-label="Nom du voyage" value={draftName} readOnly={!canEdit} onChange={(event) => { setDraftName(event.target.value); setDirty(event.target.value !== trip.name) }} /><div><span className={dirty ? 'dirty' : ''}>{dirty ? 'Non enregistré' : 'Enregistré'}</span>{canEdit && <button type="button" disabled={!dirty || busy} onClick={() => void run(async () => { await updateTrip(trip.id, { name: draftName }); await reload(trip.id) })}><Save size={13} />Sauver</button>}<TripExports onGoogle={() => void run(async () => { const result = await exportTripGoogleMaps(trip.id); result.links.forEach((link) => window.open(link.url, '_blank', 'noopener,noreferrer')) })} onGpx={() => void run(() => exportFile('gpx'))} onKmz={() => void run(() => exportFile('kmz'))} /></div><div className="trip-panel-global-actions"><b>{trip.status}</b>{canEdit && trip.status === 'draft' && <button type="button" onClick={() => void run(async () => { await startTrip(trip.id); await reload(trip.id) })}>Démarrer</button>}{canEdit && trip.status === 'in_progress' && <button type="button" onClick={() => void run(async () => { await completeTrip(trip.id); await reload(trip.id) })}>Terminer</button>}<button type="button" aria-label="Dupliquer le voyage" onClick={() => void run(async () => { const copy = await duplicateTrip(trip.id); await reload(copy.id) })}><Copy size={13} /></button>{poiMap.can_delete && <button type="button" aria-label="Supprimer le voyage" onClick={() => { if (window.confirm('Supprimer définitivement ce voyage ?')) void run(async () => { await deleteTrip(trip.id); await reload('') }) }}><Trash2 size={13} /></button>}</div></div>{canEdit && <button className="trip-panel-add-day" type="button" onClick={() => void run(async () => { await addTripDay(trip.id); await reload(trip.id) })}><CalendarPlus size={15} />Ajouter une journée</button>}<div className="trip-panel-days">{trip.days.map((day, dayIndex) => <div key={day.id}>{dayIndex === 0 && <Departure trip={trip} canEdit={canEdit} run={run} reload={reload} />}{dayIndex > 0 && <Night previous={trip.days[dayIndex - 1]} next={day} trip={trip} canEdit={canEdit} run={run} reload={reload} />}<details className="trip-panel-day" open={day.id === activeDayId} onToggle={(event) => { if (event.currentTarget.open) onActiveDayChange(day.id) }} onDragOver={(event) => event.preventDefault()} onDrop={(event) => drop(event, day)}><summary><span>{day.day_number}</span><span><strong>{day.title || `Jour ${day.day_number}`}</strong><small>{day.stops.length} étapes · {formatDistance(day.route_distance_meters)} · {formatDuration((day.route_duration_seconds ?? 0) / 60 + (day.visit_duration_minutes ?? 0))}</small></span>{canEdit && <span className="trip-panel-day-actions"><button type="button" aria-label="Monter la journée" onClick={(event) => { event.preventDefault(); reorderDays(dayIndex, -1) }}><ArrowUp size={12} /></button><button type="button" aria-label="Descendre la journée" onClick={(event) => { event.preventDefault(); reorderDays(dayIndex, 1) }}><ArrowDown size={12} /></button><button type="button" aria-label="Dupliquer la journée" onClick={(event) => { event.preventDefault(); void run(async () => { await duplicateTripDay(day.id); await reload(trip.id) }) }}><Copy size={12} /></button><button type="button" aria-label="Supprimer la journée" onClick={(event) => { event.preventDefault(); if (window.confirm('Supprimer cette journée ?')) void run(async () => { await deleteTripDay(day.id); await reload(trip.id) }) }}><Trash2 size={12} /></button></span>}</summary><div className="trip-panel-day-content">{day.route_status === 'stale' && <p>Ordre modifié — recalculez l’itinéraire.</p>}<ul>{day.stops.map((stop, index) => <li key={stop.id} className={`${draggedStopId === stop.id ? 'is-dragging' : ''}${dropTarget?.dayId === day.id && dropTarget.index === index ? ' drop-before' : ''}${index === day.stops.length - 1 && dropTarget?.dayId === day.id && dropTarget.index === day.stops.length ? ' drop-after' : ''}`} draggable={canEdit} onDragStart={(event) => { event.dataTransfer.effectAllowed = 'move'; event.dataTransfer.setData('text/plain', `stop:${stop.id}`); setDraggedStopId(stop.id) }} onDragEnd={() => { setDraggedStopId(null); setDropTarget(null) }} onDragOver={(event) => { if (!canEdit) return; event.preventDefault(); event.stopPropagation(); const bounds = event.currentTarget.getBoundingClientRect(); setDropTarget({ dayId: day.id, index: index + Number(event.clientY > bounds.top + bounds.height / 2) }) }} onDrop={(event) => dropStop(event, day, dropTarget?.dayId === day.id ? dropTarget.index : index)}><GripVertical size={13} /><i>{index + 1}</i><span role="button" tabIndex={0} onClick={() => onStopFocus?.(stop.latitude, stop.longitude)} onKeyDown={(event) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); onStopFocus?.(stop.latitude, stop.longitude) } }}><strong>{stop.name}</strong><small>{stop.visit_duration_minutes ?? 0} min · {stop.is_required ? 'Obligatoire' : 'Facultative'}</small></span>{stop.is_locked && <Lock size={11} />}{canEdit && <span className="trip-panel-stop-actions"><button type="button" aria-label="Monter l’étape" onClick={(event) => { event.stopPropagation(); reorderStops(day, index, -1) }}><ArrowUp size={11} /></button><button type="button" aria-label="Descendre l’étape" onClick={(event) => { event.stopPropagation(); reorderStops(day, index, 1) }}><ArrowDown size={11} /></button><button type="button" aria-label="Supprimer l’étape" onClick={(event) => { event.stopPropagation(); void run(async () => { await deleteTripStop(stop.id); await reload(trip.id) }) }}><Trash2 size={11} /></button></span>}</li>)}</ul>{day.stops.length === 0 && <p className="trip-panel-drop">Glissez des POI depuis le panneau Lieux</p>}{canEdit && <FreeStop day={day} poiMap={poiMap} reload={() => reload(trip.id)} />}<div className="trip-panel-route-actions"><button className={routeFeedback === day.id ? 'route-success' : undefined} type="button" disabled={!canCalculateRoute(trip, day, dayIndex)} onClick={() => recalculateRoute(day)}>{routeFeedback === day.id ? <><Check size={13} />Itinéraire rafraîchi</> : <><Route size={13} />Itinéraire</>}</button><button type="button" disabled={day.stops.length < 2} onClick={() => void run(async () => setOptimization({ dayId: day.id, value: await optimizeTripDay(day.id) }))}><Navigation size={13} />Optimiser</button></div>{optimization?.dayId === day.id && <div className="trip-panel-optimization"><span>Avant {formatDuration(optimization.value.before / 60)}</span><span>Après {formatDuration(optimization.value.after / 60)}</span><b>Gain {formatDuration(optimization.value.gain / 60)}</b><button type="button" onClick={() => setOptimization(null)}>Refuser</button><button type="button" onClick={() => void run(async () => { await confirmTripOptimization(day.id, optimization.value.optimized_stop_ids); setOptimization(null); await reload(trip.id) })}><Check size={11} />Accepter</button></div>}</div></details></div>)}</div></>}
    {createOpen && <CreateTripDialog mapName={poiMap.name} onClose={() => setCreateOpen(false)} onCreate={async (payload) => { const created = await createTrip(poiMap.id, payload); await reload(created.id); setCreateOpen(false) }} />}
  </aside>
}

function TripExports({ onGoogle, onGpx, onKmz }: { onGoogle: () => void; onGpx: () => void; onKmz: () => void }) { return <details className="trip-panel-exports"><summary><Download size={13} /></summary><div><button type="button" onClick={onGoogle}>Google Maps</button><button type="button" onClick={onGpx}>GPX</button><button type="button" onClick={onKmz}>KMZ</button></div></details> }
function FreeStop({ day, poiMap, reload }: { day: TripDay; poiMap: PoiMap; reload: () => Promise<void> }) {
  const [open, setOpen] = useState(false)
  const anchor = day.stops.at(-1)
  return <><button className="trip-panel-free-stop" type="button" onClick={() => setOpen(true)}><GripVertical size={13} /><i>+</i><span><strong>Lieu libre</strong><small>Ajouter une adresse ou des coordonnées</small></span><Plus size={13} /></button>
    {open && <CreateTripNightDialog kind="stop" mapName={poiMap.name} countryCode={poiMap.country.iso_alpha2} focus={[anchor?.latitude ?? poiMap.effective_center_latitude, anchor?.longitude ?? poiMap.effective_center_longitude]} onClose={() => setOpen(false)} onCreate={async (payload) => { await addTripStop(day.id, { ...payload }); await reload(); setOpen(false) }} />}
  </>
}
function Departure({ trip, canEdit, run, reload }: { trip: Trip; canEdit: boolean; run: (action: () => Promise<void>) => Promise<void>; reload: (id?: string) => Promise<void> }) {
  const [dialog, setDialog] = useState<{ placeId?: string; edit?: boolean } | null>(null)
  const anchor = trip.days[0]?.stops[0]
  const drop = (event: DragEvent) => { event.preventDefault(); if (trip.departure || !canEdit) return; const value = event.dataTransfer.getData('text/plain'); if (value.startsWith('place:')) setDialog({ placeId: value.slice(6) }) }
  return <><div className={`trip-panel-night trip-panel-departure${!trip.departure && canEdit ? ' drop-enabled' : ''}`} onDragOver={(event) => { if (!trip.departure && canEdit) event.preventDefault() }} onDrop={drop}><MapPin size={14} /><span><strong>Départ</strong><small>{trip.departure?.name ?? 'Glissez un POI ou ajoutez un point de départ'}</small></span>{canEdit && (trip.departure ? <span className="trip-anchor-actions"><button type="button" aria-label="Modifier le point de départ" onClick={() => setDialog({ edit: true })}><Pencil size={11} /></button><button type="button" aria-label="Supprimer le point de départ" onClick={() => void run(async () => { await deleteTripDeparture(trip.departure!.id); await reload(trip.id) })}><Trash2 size={11} /></button></span> : <button type="button" onClick={() => setDialog({})}><Plus size={11} />Ajouter</button>)}</div>
    {dialog && <CreateTripNightDialog kind="departure" mode={dialog.edit ? 'edit' : 'create'} focus={[anchor?.latitude ?? 46.2276, anchor?.longitude ?? 2.2137]} initialPlaceId={dialog.placeId ?? (dialog.edit ? trip.departure?.place_id ?? undefined : undefined)} initialLocation={dialog.edit && trip.departure && !trip.departure.place_id ? trip.departure : undefined} initialNotes={dialog.edit ? trip.departure?.notes : undefined} initialDepartureTime={dialog.edit ? trip.departure?.departure_time : undefined} onClose={() => setDialog(null)} onCreate={async (payload) => { if (dialog.edit && trip.departure) await updateTripDeparture(trip.departure.id, payload); else await addTripDeparture(trip.id, payload); await reload(trip.id); setDialog(null) }} />}
  </>
}
function Night({ trip, previous, next, canEdit, run, reload }: { trip: Trip; previous: TripDay; next: TripDay; canEdit: boolean; run: (action: () => Promise<void>) => Promise<void>; reload: (id?: string) => Promise<void> }) {
  const night = trip.nights.find((item) => item.previous_day_id === previous.id && item.next_day_id === next.id)
  const [dialog, setDialog] = useState<{ placeId?: string; edit?: boolean } | null>(null)
  const anchor = previous.stops.at(-1) ?? next.stops[0]
  const drop = (event: DragEvent) => { event.preventDefault(); if (night || !canEdit) return; const value = event.dataTransfer.getData('text/plain'); if (value.startsWith('place:')) setDialog({ placeId: value.slice(6) }) }
  return <><div className={`trip-panel-night${!night && canEdit ? ' drop-enabled' : ''}`} onDragOver={(event) => { if (!night && canEdit) event.preventDefault() }} onDrop={drop}><BedDouble size={14} /><span><strong>Nuit {previous.day_number}</strong><small>{night?.name ?? 'Glissez un POI ou ajoutez un hébergement'}</small></span>{canEdit && (night ? <span className="trip-anchor-actions"><button type="button" aria-label="Modifier l’hébergement" onClick={() => setDialog({ edit: true })}><Pencil size={11} /></button><button type="button" aria-label="Supprimer l’hébergement" onClick={() => void run(async () => { await deleteTripNight(night.id); await reload(trip.id) })}><Trash2 size={11} /></button></span> : <button type="button" onClick={() => setDialog({})}><Plus size={11} />Ajouter</button>)}</div>
    {dialog && <CreateTripNightDialog previousDayId={previous.id} nextDayId={next.id} mode={dialog.edit ? 'edit' : 'create'} focus={[anchor?.latitude ?? 46.2276, anchor?.longitude ?? 2.2137]} initialPlaceId={dialog.placeId ?? (dialog.edit ? night?.place_id ?? undefined : undefined)} initialLocation={dialog.edit && night && !night.place_id ? night : undefined} initialNotes={dialog.edit ? night?.notes : undefined} initialCheckInTime={dialog.edit ? night?.check_in_time : undefined} initialCheckOutTime={dialog.edit ? night?.check_out_time : undefined} onClose={() => setDialog(null)} onCreate={async (payload) => { if (dialog.edit && night) await updateTripNight(night.id, payload); else await addTripNight(trip.id, payload); await reload(trip.id); setDialog(null) }} />}
  </>
}
function formatDistance(value: number | null) { const amount = value ?? 0; return amount >= 1000 ? `${(amount / 1000).toFixed(1)} km` : `${Math.round(amount)} m` }
function formatDuration(value: number) { const minutes = Math.round(value); return minutes >= 60 ? `${Math.floor(minutes / 60)} h ${String(minutes % 60).padStart(2, '0')}` : `${minutes} min` }
function canCalculateRoute(trip: Trip, day: TripDay, dayIndex: number) {
  const hasStart = dayIndex === 0 ? trip.departure !== null : trip.nights.some((night) => night.next_day_id === day.id)
  const hasEnd = trip.nights.some((night) => night.previous_day_id === day.id)
  return day.stops.length + Number(hasStart) + Number(hasEnd) >= 2
}
