---
import { Check } from '@lucide/astro';
interface Props { items: string[]; }
const { items } = Astro.props;
---
<ul class="feature-list">
  {items.map((item) => <li><span aria-hidden="true"><Check size={15} stroke-width={2.4} /></span>{item}</li>)}
</ul>
